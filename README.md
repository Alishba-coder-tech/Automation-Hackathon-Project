# Automation-Hackathon-Project
When a candidate applies and selects a job role, the system automatically downloads their resume and retrieves the correct job description. It then uses AI to compare the resume against the job requirements, generating a suitability score.
